jQuery(function() {
  var Game  = window.Game;
  var X     = window.X;
  var O     = window.O;
  var Empty = window.Empty;

  var game = new Game({x: X, o: O, empty: Empty});

  $(".space").click(function() {
    jQuery.get("move", {board : "", move : "a1"}).success(function(data){
      console.log(data);
    });
  });
});
