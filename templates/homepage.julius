jQuery(function() {
  $(".space").click(function() {
    var board = "";
    jQuery(".space").each(function() {
      board = board + $(this).html();
    });

    jQuery.get("move", {board : board, move : "a1"}).success(function(data){
      console.log(data);
    });
  });
});
